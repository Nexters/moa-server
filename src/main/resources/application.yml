spring:
  config:
    import:
      - moa-secret/oauth.yml
      - moa-secret/jwt.yml
      - moa-secret/db.yml
      - moa-secret/admin.yml
      - moa-secret/discord.yml

  profiles:
    default: local
